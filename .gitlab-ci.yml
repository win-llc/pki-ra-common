image: docker:latest
services:
  - name: docker:dind
    command: ["--insecure-registry=registry.gitlab.winllc-dev.com"]

variables:
  DOCKER_TLS_CERTDIR: ""

stages:
  - build
  - publish

build:
  image: gradle:7.1-jdk16
  stage: build
  script:
    - gradle build
  artifacts:
    paths:
      - build/libs/*.jar

publish-artificat:
  image: gradle:7.1-jre16
  stage: publish
  script:
    - gradle uploadArchives